# Setup the Minikube, go to scripts/minikube setup in the repo
# Setup the kubectl, go to scripts/kubectl setup in the repo
# Setup the docker, go to scripts/docker setup in the repo
# Start the minikube cluster
minikube start --memory=6144 --cpus=4 --driver=docker --force


